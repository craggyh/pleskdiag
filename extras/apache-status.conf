# Webcore PleskDiag - local Apache mod_status endpoint.
# The PleskDiag collector connects only over loopback.
ExtendedStatus On
<Location "/pleskdiag-server-status">
    SetHandler server-status
    Require local
</Location>
